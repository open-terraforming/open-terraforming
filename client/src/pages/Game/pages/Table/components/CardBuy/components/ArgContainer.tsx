import styled from 'styled-components'

export const ArgContainer = styled.div`
	display: flex;
	margin: 0.5rem 0;
	align-items: center;
	justify-content: center;

	select,
	input,
	button {
		margin: 0 0.4rem;
	}

	input[type='number'] {
		width: 64px;
	}
`
